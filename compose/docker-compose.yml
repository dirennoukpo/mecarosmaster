version: "3.9"
services:
  mecarosmaster:
    build:
      context: ./service/mecarosmaster
      dockerfile: Dockerfile
    env_file:
      - ../env/.env
    devices:
      - "${SERIAL_PORT}:${SERIAL_PORT}"
      - "${CAMERA_PORT}:${CAMERA_PORT}"
